      <div class="nav-birds">

        <h2 class="navbar"><%= link_to "Spot the Bird", daily_bird_path %></h2>

        <div class="nav-links">
          <%= button_to "Profile", profile_path(current_user), method: :get, class: "nav-btn" %>
          <%= button_to "Settings", settings_path(current_user), method: :get, class: "nav-btn" %>
          <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "nav-btn" %>
          <%= button_to "New spot", new_user_spot_path(current_user), method: :get, class: "spot-btn" %>
        </div>

        <!--
        <div class="dropdown dropleft navbar-dropdown">
          <button type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Menu
          </button>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <%#= link_to "Settings", edit_user_registration_path, class: "dropdown-item" %>
            <%#= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </div>
        -->

      </div>
